{
    "builds": [{
        "src": "MedicalSearch/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "MedicalSearch/wsgi.py"
        }
    ]
}